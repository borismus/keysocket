keySocket.init(
    "digitallyimported",
    {
        "play-pause": "#webplayer-region .controls a:first-child"
        // prev is skipped
        // next is skipped
        // stop is omitted
    }
);