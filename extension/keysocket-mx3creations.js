keySocket.init(
    "mx3creations",
    {
        "play-pause": "#player-container .mejs__playpause-button button"
        // next is omitted
        // prev is omitted
        // stop is omitted
    }
);
