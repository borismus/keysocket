// not tested (regional restrictions)

keySocket.init(
    "superplayer",
    {
        "play-pause": "[data-function=play]",
        // prev is skipped
        "next": "[data-function=next]"
        // stop is omitted
    }
);