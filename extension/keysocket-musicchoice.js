// not tested (account is needed)

keySocket.init(
    "musicchoise",
    {
        "play-pause": "#audiomute"
        // prev is skipped
        // next is skipped
        // stop is omitted
    }
);