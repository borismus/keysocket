keySocket.init(
    "openings-ninja",
    {
        "play-pause": ".plyr__controls button",
        "next": "#controllers .btn-group button:not(#themes-button):not(#mirror-button)",
        // prev is omitted
        // stop is omitted
    }
);