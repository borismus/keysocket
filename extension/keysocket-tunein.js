keySocket.init(
    "tunein",
    {
        "play-pause": "[data-testid=playButtonWrapper] svg",
        "stop": "[data-testid=player-status-playing]"
        // prev is omitted
        // next is omitted
    }
);
