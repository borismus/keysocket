keySocket.init(
    "tunein",
    {
        // function example
        "play-pause": "[data-testid=profilePlayerActionButton]"
        // prev is skipped
        // next is skipped
        // stop is omitted
    }
);
